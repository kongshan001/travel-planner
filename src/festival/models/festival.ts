export interface Festival {
  id: string;
  name: string;
  nameLocal: string;
  country: string;
  city: string;
  month: number;
  dayStart: number;
  durationDays: number;
  description: string;
  tips: string[];
}
